# 0.10.0 验证记录

Blender 5.1.2，2026-09-07。

- 道具持握逐帧世界矩阵误差 0；撤回恢复原状态，已有道具动画保护通过。
- 多个不明确的 Action 槽位选择失败时回滚；单一/明确槽位正常绑定。
- 分数 FPS 元数据按真实 FPS 保存，场景 FPS 不改变。
- 接缝、脚接触、目标角色切换、根运动空间、末端保护、缓冲规则回归通过。
- 真实工程接缝测试：边界差异约 6.31° 降至 1.86°；重复操作、成对恢复、失败回滚和人工修改保护通过。
- 真实工程脚接触测试：约 0.16 m 漂移压低到 0.000002 m 内；撤回后原动作恢复。
- 从真实用户扩展目录加载新版，版本、操作符、属性和面板注册验证通过。

真实工程测试只操作内存副本。脚接触和接缝数字属于测试样本，不表示对所有生成动作都有相同效果。生成模型的手指表现和道具碰撞仍需人工检查。

运行普通回归时使用 Blender `--background --factory-startup --python tests/finishing_test.py`。真实工程用例需要现有工程夹具；不要把测试脚本直接用于正在编辑的工程窗口。安装配置检查使用 `--background --python tests/profile_smoke.py`，不保存偏好。

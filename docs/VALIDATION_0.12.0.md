# 0.12.0 验证

Blender 5.1.2，2026-09-12。本次不修改用户工程，不重新渲染。

- `delivery_assets_test.py`：缺失依赖在首次修改前阻断；中途失败回滚；已有内嵌不动；图片/声音/字体打包；重复操作；序列外部提示；真实缺库与追加来源区分；另存后原素材不在仍能读取；动作、网格、负帧及FPS保留。
- `render_plan_test.py`：0–778共779帧分17段，末段11帧；-30起点区间不遗漏；非法范围拒绝；分数FPS与实际负帧关键帧保留；源hash变更、未内嵌资源、实时刚体和非1帧步长阻断；注册/卸载往返。
- 原有 `finishing_test.py` 通过，道具误差0、槽位歧义回滚、分数FPS保留。
- 原有 `physics_synthetic_test.py` 通过，安全物理工作进程与恢复行为保持。
- 最终厨房工程只读检查：73项内嵌图片，1项内部临时图，缺失/外部/待核验为0，6条历史来源；工程未保存。
- 独立新进程核对实际安装路径、启用状态、扩展清单版本和逐文件哈希，注册/卸载往返通过。

规划JSON不代表已经渲染，也不证明物理或画面自然。资源检查无法保证任意第三方插件字符串路径已覆盖；视频序列和外部模拟缓存需另行交付核验。详见 DELIVERY_GUIDE.md。

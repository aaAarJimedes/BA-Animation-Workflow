# 0.14.0

- 将动作制作从主面板移入独立折叠区；主面板显示真实帧率与范围，不再把生成依赖缺失显示成所有功能错误。
- 部件分离显示勾选面数、按状态启用按钮、默认聚焦服装；更换目标、缺失源网格和无效活动组有明确拦截。面级复核状态不因局部修改而全部消失。
- 每次识别预计算骨骼名称分类，批量读取顶点坐标、合并权重哈希，兼容旧版指纹；不缓存可过期的几何数据。
- 组件状态一次扫描；纯文字换行采用有界缓存，预览没有变化时不强制更新依赖图。
- 快速预览保留 FPS/fps_base 与时间范围；物理高级参数折叠、无效烘焙按钮禁用、进度仅变化时刷新并归属实际任务场景。
- Blender 5.1.2 真实人物识别中位耗时 0.803→0.354 秒；校验 0.170→0.105 秒。
- 桌面视觉复查修正窄侧栏中文说明截断；实际验证人物识别、彩色预览及原材质恢复。

# 0.13.0（本地开发版）

- 新增人物部件候选识别、独立彩色预览、面级人工纠正与自定义分组。
- 新增保留原网格的事务式分离，按部件归入子集合；支持多个源网格保持各自形态键系统。
- 验证顶点、全部形态键、UV、权重、材质及绑定，恢复自定义法线；保留形态键 Action、简单 NLA 与自引用驱动。
- 提供原模型/分离结果切换、源数据变化拦截、失败回滚和临时预览清理；不复制物理系统或自动补身体。
- 同步面板与导出元数据版本至 0.13.0。细节和限制见 docs/PARTS_GUIDE.md。

# 0.12.0

- 新增外部资源报告和可回滚的本地媒体打包；区分实际依赖与追加来源，明确链接库、视频和缓存的交付边界。
- 新增源哈希绑定的分段渲染计划，保留真实 FPS 与预热，检查实时模拟和帧步长；不自动启动渲染或拼接。
- 更新工作台版本显示、当前安装说明与工作区构建路径，保留已有单动作、接缝、道具及物理功能。
- 将妈妈厨房项目验证过的渲染、归档与模型诊断经验纳入可复用技能。
# 0.11.0

- 新增独立的 MMD 物理检查、可取消的隔离模拟、逐帧回放验证与可恢复的物理 Action。
- 保留 FPS、完整动作和负帧预热；从正帧连续模拟后映回原时间，规避 Blender 5.1 点缓存的第 0 帧问题。
- 复用 MMD Tools 的构建规则；只在副本中校正有充分关节证据的统一布局偏移，拒绝不支持或含混的输入。
- 原动作、相机和道具曲线保持不变；真实 809 帧工程及两类 MMD 动态骨骼通过回归。

安装 `ba_animation_workflow-0.11.0.zip`。使用说明见 `docs/PHYSICS_GUIDE.md`，适用范围和验证记录见 `docs/VALIDATION_0.11.0.md`。

# 0.10.0

BA Animation Workflow 0.10.0：单段动作输入、道具接触与收尾检查。

- 一个 Clip 对应一段动作提示，显示 N/1000 字符及当前 FPS 下的时长；拒绝把 Markdown/YAML 制作方案误当动作提示提交。
- 新增独立道具持握/放下烘焙及恢复，保护已有动画；新增只读低速区间和手部/头部速度检查。
- 拆分单段输入与时间计划模块，统一 Action 读取，保留接缝、防脚滑、旧工程属性和重复操作恢复。
- 修复分数 FPS 元数据精度和中文帮助文字截断。

Blender 5.1.2 验证通过：实际安装配置、42 个原有操作符、道具接触/撤回、模糊槽位回滚、帧率、真实工程接缝与脚接触回归。数字与适用限制见仓库 docs/VALIDATION_0.10.0.md。

安装 ba_animation_workflow-0.10.0.zip；不要使用 GitHub 的 Source code ZIP 安装。第三方生成插件需独立安装。
